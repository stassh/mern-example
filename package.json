{
  "name": "mern-tut",
  "version": "1.0.0",
  "description": "Tutorial application",
  "main": "server.js",
  "scripts": {
    "compile": "babel src --presets es2015,react --out-dir static/lib",
    "watch": "babel src --presets es2015,react --out-dir static/lib --watch ",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon -w server.js server.js"
  },
  "keywords": [
    "MERN"
  ],
  "author": "Stanislav",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.2"              //simple http server
  },
  "devDependencies": {
    "babel-cli": "^6.24.0",           //jsx->js babel parser cli tools
    "babel-preset-es2015": "^6.24.0", //jsx->js es2015 for babel
    "babel-preset-react": "^6.23.0",  //React support for babel
    "nodemon": "^1.11.0"              //auto restart server
  }
}
